{
  "name": "cattle-tracker",
  "version": "0.6.18",
  "description": "Учёт коров — веб, десктоп, мобильные",
  "scripts": {
    "build": "vite build",
    "test": "vitest run tests/",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:headed": "playwright test --headed",
    "e2e:electron": "node e2e/run-electron-tests.js",
    "e2e:electron:ui": "node e2e/run-electron-tests.js --ui",
    "e2e:electron:log": "node e2e/run-electron-tests-with-log.js",
    "start": "echo Open index.html in browser or run server",
    "start:web": "npx serve -p 9323 .",
    "server": "cd server && npm start",
    "electron": "cd electron && npm start",
    "electron:dist": "cd electron && npm run dist",
    "version:sync": "node scripts/sync-version.js",
    "installer": "npm run version:sync && npm run build && npm run electron:dist",
    "mobile:installer": "node scripts/mobile-installer.js",
    "mobile:upload": "node scripts/mobile-installer.js --upload-only",
    "cap:sync": "cap sync",
    "apk": "npm run version:sync && npm run build && npx cap sync android",
    "apk:bump": "node scripts/bump-version.js",
    "apk:release": "node scripts/bump-version.js && npm run apk",
    "cap:android": "cap run android",
    "cap:ios": "cap run ios"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.49.0",
    "@types/node": "^25.2.3",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "eslint": "^10.0.0",
    "globals": "^17.3.0",
    "serve": "^14.2.4",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  },
  "capacitor": {
    "appId": "com.cattletracker.app",
    "appName": "Учёт коров",
    "webDir": "dist"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.3",
    "@capacitor/browser": "^6.0.5",
    "fflate": "^0.8.2",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "xlsx": "0.18.5"
  }
}
