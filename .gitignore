# Dependencies
node_modules/

# Build output (Vite)
dist/

# Electron (dependencies and build output — too large for GitHub)
electron/node_modules/
electron/dist/
electron/dist-rel/
electron/dist-build-*/

# OS
.DS_Store
Thumbs.db

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
.cursor/debug.log

# Env / secrets (optional)
.env
.env.local
# Секреты для npm run mobile:installer (логин admin или JWT)
mobile-installer.env
client_secret*.json

# Server deploy (local IP, do not commit)
server/server-address.txt
server/deploy.env

# Выкладываемые на сервер APK и служебные файлы (не коммитить)
server/apk/*
!server/apk/.gitkeep

# Локальная копия собранного Android APK у корня (после assemble*)
apk/*
!apk/.gitkeep

# Playwright
test-results/
playwright-report/
playwright/.cache/
# Локальный путь к exe приложения для e2e:electron (скопируйте в electron-exe-path.txt)
e2e/electron-exe-path.txt