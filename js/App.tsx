/**
 * React root component. Legacy screens remain in index.html and are controlled by window.navigate.
 * This shell allows gradual migration of screens to React components.
 */
import React from 'react';

export default function App(): React.ReactElement {
  return <></>;
}
