---
name: План №9 Улучшения
overview: "План №9: настройка полей и шаблоны, экспорт с выбором полей и листов (осеменения/задачи), закрепление шапки и кнопка «Вверх», унификация кнопок, УЗИ в меню, «Уведомления и планы» / «Список задач», статистика (%), «Дни стельности», уведомление «Проверить отел», единый источник полей, роутинг, доступность, виртуализация списка, валидация дат, офлайн, единый стиль кнопок. Финально — версия v1.2.4 и сборка установочного файла."
todos:
  - id: field-config
    content: Единый источник полей (field-config.js), интеграция в список/карточку/экспорт
    status: in_progress
  - id: days-pregnant
    content: Поле «Дни стельности» — функция, карточка, список, уведомление «Проверить отел»
    status: pending
  - id: view-fields-templates
    content: Настройка полей — все поля + шаблоны (сохранение/применение)
    status: pending
  - id: export-dialog
    content: Экспорт — диалог выбора полей, шаблоны экспорта, галочки листов осеменений и задач
    status: pending
  - id: buttons-style
    content: Унификация кнопок карточки и единый стиль кнопок по приложению
    status: pending
  - id: uzi-menu
    content: Кнопка УЗИ в меню «Действия»
    status: pending
  - id: stats-percents
    content: Вторая строка статистики — проценты структуры стада
    status: pending
  - id: notifications-tasks
    content: «Уведомления и планы», отдельный экран «Список задач»
    status: pending
  - id: sticky-scroll-top
    content: Закрепление панели/шапки в списке, кнопка «Вверх»
    status: pending
  - id: routing
    content: Роутинг по hash/history и синхронизация состояния
    status: pending
  - id: a11y
    content: Доступность — aria-label, фокус в модалках, навигация с клавиатуры
    status: pending
  - id: virtualization
    content: Виртуализация таблицы списка животных
    status: pending
  - id: date-validation
    content: Валидация дат в формах (осеменение, отёл, УЗИ)
    status: pending
  - id: offline
    content: Индикатор офлайн и обработка online/offline
    status: pending
  - id: build-version
    content: Версия v1.2.4 в package.json и сборка npm run dist в electron
    status: pending
isProject: false
---

# План №9: Улучшения приложения

## Текущее состояние (кратко)

- **Карточка животного** ([electron/js/view-cow.js](electron/js/view-cow.js)): ~20 полей в разметке (Кличка, Группа, Дата рождения, Лактация, Дата отёла, Дата осеменения, Номер попытки, Бык, Осеменитель, Код, Статус, Дата выбытия, Начало сухостоя, ПДО, Протокол, Начало протокола, Примечание, Синхронизация, Дата добавления, Изменено пользователем). Кнопки действий: «Редактировать» (класс `small-btn edit`), «Запуск»/«Отел»/«УЗИ»/«История» (`small-btn`), «Назад к списку» (`back-button`).
- **Настройка полей** ([electron/js/view-list.js](electron/js/view-list.js)): массив `VIEW_LIST_FIELDS` из **12** полей (cattleId, nickname, group, lactation, inseminationDate, bull, attemptNumber, status, calvingDate, dryStartDate, note, synced). Сохранение в `localStorage` по ключу `VIEW_LIST_FIELDS_KEY`. Шаблонов нет.
- **Экран «Список всех животных»** ([electron/index.html](electron/index.html) — `#view-screen`): блок `.view-screen-header` (Назад, заголовок, «Настройка полей», поиск/фильтр, массовые действия) и `.view-entries-wrapper` с таблицей. В [electron/css/components-tables.css](electron/css/components-tables.css) уже заданы `position: sticky` для шапки и `overflow: auto` для обёртки; отдельной кнопки «Вверх» нет.
- **Главное меню «Действия»** ([electron/js/menu.js](electron/js/menu.js)): кнопки «Ввести осеменение», «Запуск», «Отел», «Поставить на протокол». Кнопки **«УЗИ»** в меню нет (есть только в карточке и переход `navigate('uzi')` уже поддерживается).
- **Уведомления** ([electron/js/notifications.js](electron/js/notifications.js)): экран «Уведомления», центр уведомлений и внутри него блок «Список задач (инъекции по протоколам)» с периодом и списком задач из `getProtocolTasks()`. Отдельного пункта меню «Список задач» нет.
- **Статистика стада** ([electron/js/menu.js](electron/js/menu.js) — `updateHerdStats()`): одна строка — всего коров, стельных, в сухостое, осеменённых, на брак. Второй строки с процентами нет.
- **УЗИ**: экран и логика есть в [electron/js/cow-operations.js](electron/js/cow-operations.js) (`initUziScreen`, сохранение в `entry.uziHistory`), в карточке кнопка «УЗИ» есть.
- **Дни стельности**: в коде не считаются. Для расчёта можно использовать последнее осеменение (из `inseminationHistory` или `inseminationDate`) и статус «Стельная».
- **Уведомление «Проверить отел»**: в `checkUpcomingEvents()` такой проверки нет; нужно добавить для коров со статусом «Стельная» и сроком стельности > 275 дней.

---

## 1. Настройка полей — все поля и шаблоны

**Цель:** В настройке полей должны быть все поля, доступные в карточке (и в таблице списка), плюс возможность сохранять/загружать шаблоны с названием.

**Действия:**

- В [electron/js/view-list.js](electron/js/view-list.js) расширить `VIEW_LIST_FIELDS` до полного набора полей карточки (и списка). Добавить поля, которых нет сейчас: `birthDate` (Дата рождения), `exitDate` (Дата выбытия), `protocolName` (Протокол — из `entry.protocol.name` или `entry.protocolName`), `protocolStartDate` (Начало протокола), `dateAdded` (Дата добавления), `lastModifiedBy` (Изменено пользователем). Добавить вычисляемое поле **«Дни стельности»** (см. п. 9) — ключ например `daysPregnant`, с функцией `render`, использующей общую функцию расчёта дней стельности. Итого в настройке будет больше 12 полей (все нужные для списка + новое).
- В модальном окне «Настройка полей» ([electron/index.html](electron/index.html) — `#viewFieldsSettingsModal`) добавить блок **«Шаблоны»**:
  - Список сохранённых шаблонов (название + кнопки «Применить», при желании «Удалить»).
  - Поле ввода названия и кнопка «Сохранить текущий набор как шаблон».
  - Хранение: например `localStorage` ключ `cattleTracker_viewList_fieldTemplates` — массив объектов `{ name: string, fieldKeys: string[] }`. При «Применить» записывать выбранный `fieldKeys` в `VIEW_LIST_FIELDS_KEY` и перерисовывать список.
- При первом запуске после обновления: если в `localStorage` лежит старый список из 12 ключей, оставить его; новые поля просто появятся в чекбоксах и пользователь сможет их включить или сохранить шаблон.

**Файлы:** [electron/js/view-list.js](electron/js/view-list.js), [electron/index.html](electron/index.html), при необходимости [electron/css/components-tables.css](electron/css/components-tables.css) или отдельный CSS для модалки шаблонов.

---

## 2. Закрепление панели инструментов и шапки таблицы (полноэкранный режим на ПК)

**Цель:** При просмотре «Список всех животных» во весь экран на ПК: прокручивается только тело таблицы; панель инструментов (шапка экрана + поиск/фильтр + массовые действия) и шапка таблицы остаются на экране. Если верхний блок занимает больше 35% высоты экрана — закреплять только шапку таблицы, инструменты прокручиваются.

**Действия:**

- Структура уже подходит: `.view-screen-header` (sticky) + `.view-entries-wrapper` (overflow: auto) с таблицей внутри. Нужно:
  - Обеспечить, что при полноэкранном/десктопном просмотре таблица рендерится так, чтобы **thead** оставался видимым при скролле внутри `view-entries-wrapper`. Варианты: (A) обёртка только для `tbody` с `overflow: auto` и фиксированная высота, при этом thead остаётся в sticky-шапке экрана; (B) или второй sticky — для `thead` внутри скроллируемой области, с `top` равным высоте `.view-screen-header` (чтобы thead прилипал под ним). Условие «если панель инструментов не более 35% экрана» можно реализовать через медиа-запрос (например, `min-height` viewport) или через JS при `resize`/fullscreen: если высота header > 35% window — убрать sticky у header, оставить только sticky у thead.
  - Уточнить в вёрстке: сейчас таблица целиком в `view-entries-wrapper`, поэтому thead уезжает вверх при скролле. Нужно разделить: либо два блока (фиксированный thead с дублированием колонок по ширине, скролл только tbody), либо один контейнер с `position: sticky; top: 0` для `thead` и учёт высоты верхнего блока (например через CSS-переменную или класс при fullscreen).
- Добавить класс/атрибут для «режима полного экрана на ПК» (например при `window.electronAPI` и полноэкранном окне или при ширине экрана выше порога), чтобы применять стили закрепления только когда нужно.

**Файлы:** [electron/css/components-tables.css](electron/css/components-tables.css), при необходимости [electron/js/view-list.js](electron/js/view-list.js) (обновление разметки таблицы/обёрток или установка класса на `#view-screen`), [electron/index.html](electron/index.html) при изменении структуры.

---

## 3. Кнопка «Вверх» в окне «Список всех животных»

**Цель:** Кнопка для быстрой прокрутки страницы вверх.

**Действия:**

- В разметке экрана `#view-screen` ([electron/index.html](electron/index.html)) добавить кнопку (например в углу экрана, фиксированную или внизу справа), например: `<button type="button" id="viewScrollToTopBtn" class="scroll-to-top-btn" aria-label="Вверх">↑ Вверх</button>`.
- Стили: фиксированная позиция (например справа внизу), показывать после прокрутки вниз на порог (например 200px). Скрыть при прокрутке вверх до верха.
- В [electron/js/view-list.js](electron/js/view-list.js) (или в [electron/js/app.js](electron/js/app.js)): при показе экрана `view` подписаться на скролл контейнера `#viewEntriesList` (или `view-entries-wrapper`); при скролле вниз показывать кнопку, по клику — `container.scrollTop = 0`.

**Файлы:** [electron/index.html](electron/index.html), [electron/css/components-tables.css](electron/css/components-tables.css) или [electron/css/components-buttons.css](electron/css/components-buttons.css), [electron/js/view-list.js](electron/js/view-list.js).

---

## 4. Единый дизайн кнопок действий в карточке коровы

**Цель:** Все кнопки действий в карточке (Редактировать, Запуск, Отел, Поставить на протокол, УЗИ, История, Назад к списку) — одного стиля (форма и цвет), без выделения одной кнопки другим цветом/формой.

**Действия:**

- В [electron/js/view-cow.js](electron/js/view-cow.js) у кнопки «Редактировать» убрать класс `edit` (или переопределить стили так, чтобы все кнопки в `.cow-card-actions` имели один и тот же класс, например все `small-btn` или все `action-btn` в одном стиле). Кнопку «Назад к списку» либо оставить визуально «вторичной» (например без яркого фона), но в той же форме, либо привести к тому же классу, что и остальные.
- В [electron/css/view-cow.css](electron/css/view-cow.css) или [electron/css/components-buttons.css](electron/css/components-buttons.css): задать для `.cow-card-actions button` единые стили (размер, скругление, отступы, один цвет/тип кнопки). Убрать отдельное выделение для `.cow-card-actions .edit` или заменить на общий стиль.

**Файлы:** [electron/js/view-cow.js](electron/js/view-cow.js), [electron/css/view-cow.css](electron/css/view-cow.css), [electron/css/components-buttons.css](electron/css/components-buttons.css).

---

## 5. Кнопка «УЗИ» в главном меню «Действия»

**Цель:** В окне «Действия» на главном меню добавить кнопку «УЗИ» рядом с «Отел», «Запуск» и т.д., с функцией перехода на ввод УЗИ (проверка на стельность в карточку животного).

**Действия:**

- В [electron/js/menu.js](electron/js/menu.js) в `MENU_GROUPS.actions.buttons` добавить элемент: `{ icon: '🩺', text: 'УЗИ', onclick: "navigate('uzi')" }` (после «Поставить на протокол» или между «Отел» и протоколом — по желанию). Экран и логика уже есть (`navigate('uzi')`, `initUziScreen` в cow-operations.js).

**Файлы:** [electron/js/menu.js](electron/js/menu.js).

---

## 6. «Уведомления» → «Уведомления и планы». Разделение уведомлений и «Список задач»

**Цель:** Переименовать окно «Уведомления» в «Уведомления и планы». Внутри экрана разделить блок уведомлений и блок «Список задач». Отдельный пункт главного меню «Список задач» — экран только с запланированными задачами, с возможностью сортировки и фильтрации по дате и названию задачи.

**Действия:**

- Переименование: в [electron/index.html](electron/index.html) заголовок экрана `#notifications-screen` — с «Уведомления» на «Уведомления и планы». В [electron/js/menu.js](electron/js/menu.js) в группе `notifications` изменить заголовок и текст кнопки на «Уведомления и планы». В [electron/js/notifications.js](electron/js/notifications.js) в `renderNotificationCenter` заголовок блока оставить или изменить на «Уведомления», а на экране общий заголовок — «Уведомления и планы».
- Разделение на экране «Уведомления и планы»: в разметке или в коде рендера центра уведомлений явно два блока: (1) Уведомления (текущий список уведомлений по категориям); (2) Список задач (текущий блок «Список задач (инъекции по протоколам)» с периодом и кнопками «Сегодня»/«Неделя» и т.д.). Визуально разделить секциями/заголовками.
- Отдельное окно «Список задач»:
  - В [electron/index.html](electron/index.html) добавить экран `#tasks-screen` с заголовком «Список задач» и контейнером для списка задач (фильтр по дате, по названию задачи, сортировка).
  - В [electron/js/menu.js](electron/js/menu.js) добавить в группу `notifications` (или отдельную группу) кнопку «Список задач» с `onclick: "navigate('tasks')"`.
  - В [electron/js/notifications.js](electron/js/notifications.js) (или отдельный модуль): вынести/переиспользовать `getProtocolTasks(fromDate, toDate)` и рендер списка задач. На экране «Список задач» — таблица или список с колонками: дата, название задачи (тип/препарат), корова, группа и т.д. Добавить фильтры: период (от/до), поле поиска по названию задачи (или по корове). Сортировка по дате и по названию (переключатель или клик по заголовку). При переходе `navigate('tasks')` вызывать рендер этого экрана.

**Файлы:** [electron/index.html](electron/index.html), [electron/js/menu.js](electron/js/menu.js), [electron/js/notifications.js](electron/js/notifications.js), при необходимости [electron/css/components-screens.css](electron/css/components-screens.css).

---

## 7. Вторая строка статистики на главном меню — проценты структуры стада

**Цель:** Под первой строкой статистики (всего коров, стельных, в сухостое и т.д.) добавить вторую строку такого же визуального оформления с процентными показателями структуры стада.

**Действия:**

- В [electron/index.html](electron/index.html) в блоке `.herd-stats` добавить второй ряд: контейнер с классом того же типа (например `herd-stats-row herd-stats-percents`), внутри элементы вида: «Стельные X%», «В сухостое Y%», «Осеменённые Z%», «На брак W%» (проценты от общего числа коров в текущей выборке/объекте).
- В [electron/js/menu.js](electron/js/menu.js) в `updateHerdStats()` после заполнения абсолютных значений вычислить проценты (totalCows не ноль) и записать во вторую строку. Использовать ту же выборку `list`, что и для абсолютных показателей.

**Файлы:** [electron/index.html](electron/index.html), [electron/js/menu.js](electron/js/menu.js), [electron/css/components-screens.css](electron/css/components-screens.css) при необходимости для второй строки.

---

## 8. Поле «Дни стельности» в карточке и везде

**Цель:** Добавить поле «Дни стельности» в карточку животного и во все сопутствующие места (настройка полей списка, экспорт при необходимости). Логика: если статус «Стельная» — считать дни от последнего осеменения до сегодня; иначе поле пустое или «—».

**Действия:**

- Ввести общую функцию расчёта, например `getDaysPregnant(entry)`:
  - Если у `entry` статус не содержит «Стельная» — вернуть `null` или `'—'`.
  - Иначе взять дату последнего осеменения: из `inseminationHistory` (максимальная дата) или `inseminationDate`. Разместить функцию в [electron/js/view-cow.js](electron/js/view-cow.js) или [electron/js/utils.js](electron/js/utils.js), чтобы использовать в карточке и в view-list.
  - Вычислить разницу в днях между этой датой и сегодня (по календарным дням).
- Карточка: в [electron/js/view-cow.js](electron/js/view-cow.js) в разметке `viewCow()` добавить строку «Дни стельности:» + значение `getDaysPregnant(entry)`.
- Настройка полей и список: в `VIEW_LIST_FIELDS` добавить поле с ключом `daysPregnant`, label «Дни стельности», `render: entry => getDaysPregnant(entry) ?? '—'`, sortable по числу дней.
- При необходимости добавить в экспорт Excel ([electron/js/export-excel.js](electron/js/export-excel.js)) колонку «Дни стельности».

**Файлы:** [electron/js/view-cow.js](electron/js/view-cow.js), [electron/js/view-list.js](electron/js/view-list.js), при необходимости [electron/js/utils.js](electron/js/utils.js), [electron/js/export-excel.js](electron/js/export-excel.js).

---

## 9. Уведомление «Проверить отел» при стельности более 275 дней

**Цель:** Коровы со статусом «Стельная», у которых срок стельности более 275 дней, должны попадать в уведомления с пометкой «Проверить отел».

**Действия:**

- В [electron/js/notifications.js](electron/js/notifications.js) в функции `checkUpcomingEvents()` при обходе `entries` добавить проверку: если у записи статус содержит «Стельная», вычислить дни стельности (аналогично п. 8 — от последнего осеменения до сегодня). Если дней > 275, создать уведомление с сообщением вроде «Проверить отел: корова &lt;cattleId&gt; (дней стельности: N)», категория например `calving` или `other`, с `meta` для фильтрации. Использовать ключ дедупликации (например `overdue_` + cattleId), чтобы не плодить одно и то же уведомление при каждом тике.
- Добавить в `CATEGORY_LABELS` метку для новой категории при необходимости (например «Проверка отёла»).

**Файлы:** [electron/js/notifications.js](electron/js/notifications.js). Зависимость: функция расчёта дней стельности (п. 8) должна быть доступна в этом модуле (глобально или через общий utils).

---

## 10. Экспорт в Excel: выбор полей, шаблоны полей, листы «Осеменения» и «Список задач»

**Цель:** При нажатии «Экспорт в Excel» не сразу выгружать файл, а показывать диалог выбора полей и опций. Реализовать шаблоны полей для экспорта (сохранение/выбор набора полей). Добавить галочки: выгружать ли вторым листом список осеменений и третьим листом список задач.

**Действия:**

- Перед вызовом текущей логики `exportToExcel()` показывать модальное окно (или отдельный экран/панель) «Настройка экспорта»:
  - **Выбор полей:** список полей (тот же источник, что и для списка животных — единый конфиг полей, см. п. 12). Чекбоксы для каждого поля; «Выбрать все» / «Сбросить». Выбранные поля определяют колонки первого листа «Коровы».
  - **Шаблоны полей для экспорта:** выпадающий список или блок «Шаблон» — сохранённые пресеты (название + «Применить»). Кнопка «Сохранить текущий набор как шаблон» (ввод названия). Хранение отдельно от шаблонов вида списка, например `cattleTracker_export_fieldTemplates` в localStorage.
  - **Галочки:** «Включить лист „Осеменения“» (второй лист), «Включить лист „Список задач“» (третий лист). По умолчанию можно оставить осеменения включёнными, задачи — по желанию.
- После нажатия «Экспортировать» в диалоге: собрать выбранные поля, флаги листов; вызвать функцию экспорта, которая формирует xlsx только с выбранными колонками и только с выбранными листами. Для листа «Список задач» использовать данные из `getProtocolTasks()` (период можно задать в диалоге или экспортировать на текущую неделю/месяц).
- Текущую функцию в [electron/js/export-excel.js](electron/js/export-excel.js) разбить: отдельная функция построения книги (принимает массив ключей полей, флаг includeInseminations, флаг includeTasks), вызов из диалога после выбора пользователя.

**Файлы:** [electron/js/export-excel.js](electron/js/export-excel.js), [electron/index.html](electron/index.html) (модалка настройки экспорта), [electron/js/app.js](electron/js/app.js) или [electron/js/menu.js](electron/js/menu.js) — вызов открытия диалога вместо прямого `exportToExcel()`. После внедрения единого источника полей (п. 12) список полей в диалоге брать из него.

---

## 11. Роутинг и состояние

**Цель:** Простой роутинг по hash или History API, чтобы при «Назад» и обновлении страницы не терять контекст (текущий экран, при необходимости id карточки).

**Действия:**

- Ввести единую точку входа навигации: при смене экрана записывать в `location.hash` или `history.pushState` значение (например `#menu`, `#view`, `#view-cow/123`, `#notifications`, `#tasks`). При загрузке страницы и при событии `hashchange`/`popstate` вызывать существующую функцию `navigate(screenId)` с параметром, извлечённым из hash/path.
- Соответствие: hash `#view-cow/123` → `navigate('view-cow')` и установка текущего `cattleId` для отображения карточки (например глобальная переменная или sessionStorage). Реализация в [electron/js/menu.js](electron/js/menu.js) или в отдельном [electron/js/router.js](electron/js/router.js): функция `syncRouteToScreen()` и `syncScreenToRoute()` при каждом `navigate()`.

**Файлы:** новый [electron/js/router.js](electron/js/router.js) (опционально), [electron/js/menu.js](electron/js/menu.js), [electron/index.html](electron/index.html) (подключение скрипта). При использовании hash — без изменений на сервере.

---

## 12. Единый источник полей

**Цель:** Один конфиг полей (key, label, render для значения в списке/карточке, sortable) для карточки, списка, настройки полей и экспорта.

**Действия:**

- Вынести общий массив полей в отдельный модуль, например [electron/js/field-config.js](electron/js/field-config.js): экспорт константы `COW_FIELDS` (или `ENTRY_FIELDS`) — массив объектов `{ key, label, render(entry), sortable }`. Вычисляемые поля (ПДО, Дни стельности, Протокол) задать через `render`. Подключить этот модуль первым из тех, кто использует поля.
- В [electron/js/view-list.js](electron/js/view-list.js): заменить локальный `VIEW_LIST_FIELDS` на импорт из field-config (или глобальную переменную, заданную из field-config). Видимые поля по-прежнему из localStorage/шаблона.
- В [electron/js/view-cow.js](electron/js/view-cow.js): строить разметку карточки по тому же конфигу (итерация по полям с вызовом `render(entry)`), а не хардкодом строк.
- В настройке полей и в диалоге экспорта использовать тот же конфиг для списка чекбоксов. В [electron/js/export-excel.js](electron/js/export-excel.js) формировать заголовки и строки по выбранным ключам из конфига.

**Файлы:** новый [electron/js/field-config.js](electron/js/field-config.js), [electron/js/view-list.js](electron/js/view-list.js), [electron/js/view-cow.js](electron/js/view-cow.js), [electron/js/export-excel.js](electron/js/export-excel.js), [electron/index.html](electron/index.html) (подключение field-config до view-list и view-cow).

---

## 13. Доступность (a11y)

**Цель:** У кнопок и интерактивных элементов — aria-label, у модалок — управление фокусом и ловушка фокуса; навигация с клавиатуры (Tab, Enter) в списке и в настройке полей.

**Действия:**

- Пройти по основным экранам и кнопкам: добавить `aria-label` там, где текст кнопки недостаточен или кнопка только с иконкой. У модальных окон (`#viewFieldsSettingsModal`, `#viewCowActionHistoryModal`, модалка экспорта и т.д.) убедиться в `aria-modal="true"`, `role="dialog"` и при открытии переносить фокус на первый фокусный элемент; при закрытии возвращать фокус на элемент, открывший модалку. Ловушка фокуса (Tab внутри модалки по кругу) — по желанию, можно реализовать через обработчик keydown и проверку `ev.target`.
- Список животных: строки таблицы уже с `role="button"` и `tabindex="0"`, обработать Enter/Space для перехода в карточку (в [electron/js/view-list.js](electron/js/view-list.js) уже есть `_handleViewListKeydown`). Заголовки сортировки — `role="button"`, активация по Enter/Space. Проверить видимый фокус (outline) для всех интерактивных элементов.
- Формы: связать label с input через `id`/`for`, при ошибках валидации выводить `aria-describedby` или `aria-invalid`.

**Файлы:** [electron/index.html](electron/index.html), [electron/js/view-list.js](electron/js/view-list.js), [electron/js/ui-helpers.js](electron/js/ui-helpers.js) (если есть общая функция открытия модалок), [electron/css/base.css](electron/css/base.css) (стили фокуса).

---

## 14. Производительность списка: виртуализация

**Цель:** При большом количестве записей (сотни/тысячи) рендерить только видимые строки таблицы, чтобы скролл оставался плавным.

**Действия:**

- В [electron/js/view-list.js](electron/js/view-list.js) изменить способ отрисовки таблицы: вместо одного большого `tbody` с всеми строками использовать виртуализацию — хранить массив данных и рендерить только строки, попадающие в видимую область (плюс небольшой буфер сверху/снизу). Высота контейнера скролла и высота одной строки (или фиксированная) задают количество видимых строк; при скролле пересчитывать диапазон индексов и обновлять DOM (или использовать placeholder по высоте и трансформ для имитации позиций).
- Варианты реализации: (1) собственный простой виртуализатор (скролл-контейнер с фиксированной высотой, внутренний div с `height: totalRows * rowHeight`, абсолютное позиционирование видимых строк); (2) подключить лёгкую библиотеку (например `virtual-scroller` или аналог). Учесть сортировку и фильтрацию — виртуализация по уже отфильтрованному/отсортированному массиву.
- Сохранить текущее API: `updateViewList()` по-прежнему получает данные из `getFilteredEntries()`/`getVisibleEntries()` и сортирует; внутрь передаётся массив, который отображается виртуализованно.

**Файлы:** [electron/js/view-list.js](electron/js/view-list.js), при необходимости [electron/css/components-tables.css](electron/css/components-tables.css). Если используется библиотека — добавить в index.html или package.json (если сборка через bundler).

---

## 15. Валидация дат

**Цель:** При вводе дат (осеменение, отёл, УЗИ, начало сухостоя и т.д.) проверять непротиворечивость и показывать предупреждение без жёсткой блокировки сохранения.

**Действия:**

- Ввести вспомогательные функции (например в [electron/js/utils.js](electron/js/utils.js) или [electron/js/cow-operations.js](electron/js/cow-operations.js)): сравнение дат (календарно), проверки вида «дата осеменения не позже даты УЗИ», «дата отёла не раньше последнего осеменения», «дата начала сухостоя не после даты отёла» и т.д.
- В формах сохранения: при submit (осеменение, отёл, УЗИ, запуск) после сбора значений вызвать проверки. При нарушении показать `confirm()` или неблокирующее сообщение (toast/под полем): «Дата УЗИ раньше последнего осеменения. Всё равно сохранить?». При подтверждении — сохранять.
- По возможности подсвечивать поля с «подозрительными» датами (например красная обводка или иконка), не блокируя отправку.

**Файлы:** [electron/js/utils.js](electron/js/utils.js) или [electron/js/cow-operations.js](electron/js/cow-operations.js), [electron/js/insemination.js](electron/js/insemination.js), формы в [electron/index.html](electron/index.html) при необходимости добавления контейнеров под сообщения об ошибках.

---

## 16. Офлайн-режим и индикатор

**Цель:** При отключении сети показывать индикатор «Офлайн» и ставить синхронизацию в очередь.

**Действия:**

- Подписаться на события `window.addEventListener('online', ...)` и `window.addEventListener('offline', ...)`. При переходе в офлайн показывать в шапке или в углу экрана индикатор (блок с текстом «Офлайн» или иконка). При переходе в онлайн — скрывать или менять на «Синхронизация…».
- Логику синхронизации не менять кардинально: при появлении сети можно автоматически вызвать существующую функцию отправки несинхронизированных данных (если она есть в [electron/js/sync.js](electron/js/sync.js) или api-client). Если запросы к API при офлайне уже обрабатываются (catch), достаточно не показывать жёсткую ошибку, а показывать индикатор офлайн и при следующем онлайне повторить попытку.
- Сервис-воркер и манифест уже есть; при необходимости обновить кэш статики для офлайн-работы веб-части (если приложение также открывается в браузере).

**Файлы:** [electron/js/app.js](electron/js/app.js) или [electron/js/sync.js](electron/js/sync.js), [electron/index.html](electron/index.html) (блок индикатора офлайн), [electron/css/components-screens.css](electron/css/components-screens.css).

---

## 17. Единый стиль кнопок в приложении

**Цель:** Распространить выбранный стиль кнопок (после унификации кнопок карточки) на все экраны: Действия, формы, модалки, списки.

**Действия:**

- Определить два–три класса кнопок: первичная (например `.action-btn` или `.btn-primary`), вторичная (`.small-btn` или `.btn-secondary`), возможно «назад» (`.back-button`). В [electron/css/components-buttons.css](electron/css/components-buttons.css) задать единые размеры, скругление, отступы, цвета для этих классов.
- Пройти по [electron/index.html](electron/index.html) и скриптам: заменить разрозненные классы кнопок на выбранные (например везде кнопки действий в меню и формах — один класс, «Отмена»/«Назад» — другой). В карточке животного после п. 4 уже будет единый стиль; применить тот же набор классов на экранах осеменения, отёла, УЗИ, протокола, настроек, резервных копий и т.д.
- Убедиться, что кнопки в модалках (Настройка полей, экспорт, история) используют те же классы.

**Файлы:** [electron/css/components-buttons.css](electron/css/components-buttons.css), [electron/index.html](electron/index.html), [electron/js/view-cow.js](electron/js/view-cow.js), [electron/js/menu.js](electron/js/menu.js) (рендер кнопок подменю), остальные экраны с кнопками.

---

## 18. Сборка установочного файла и версия v1.2.4

**Цель:** По окончании реализации плана обновить версию приложения до v1.2.4 и собрать установочный файл.

**Действия:**

- В [electron/package.json](electron/package.json) в поле `"version"` заменить текущее значение на `"1.2.4"`.
- В корне проекта при наличии отдельного `package.json` с полем `version` (например для веб-части или общего манифеста) также выставить версию 1.2.4 при необходимости.
- Запустить сборку из папки `electron`: команда `npm run dist` (в [electron/package.json](electron/package.json) уже задана: `"dist": "node copy-web.js && electron-builder"`). Она копирует веб-файлы и собирает установщик. Для Windows получится установочный файл в каталоге `electron/dist` (например `cattle-tracker-desktop-setup-1.2.4.exe` по текущему artifactName).

**Файлы:** [electron/package.json](electron/package.json). Выполнение: терминал, рабочая директория `d:\VScode\cattle-tracker\electron`, команда `npm run dist`.

---

## Порядок реализации (рекомендуемый)

1. Единый источник полей (п. 12) — затем настройка полей, экспорт и карточка опираются на него.
2. Дни стельности: функция + использование в field-config (п. 8) — нужно для п. 9 и для списка/экспорта.
3. Уведомление «Проверить отел» (п. 9).
4. Расширение полей и настройка полей — все поля + шаблоны (п. 1).
5. Экспорт с диалогом выбора полей, шаблоны экспорта, галочки листов осеменений и задач (п. 10).
6. Кнопка УЗИ в меню (п. 5), унификация кнопок карточки (п. 4), единый стиль кнопок по приложению (п. 17).
7. Вторая строка статистики (п. 7).
8. Переименование и разделение «Уведомления и планы» + отдельный экран «Список задач» (п. 6).
9. Закрепление панели/шапки в списке (п. 2) и кнопка «Вверх» (п. 3).
10. Роутинг/состояние (п. 11).
11. Доступность (п. 13).
12. Виртуализация списка (п. 14).
13. Валидация дат (п. 15).
14. Офлайн-индикатор (п. 16).
15. Версия 1.2.4 и сборка: правка `package.json`, команда `npm run dist` в папке electron (п. 18).