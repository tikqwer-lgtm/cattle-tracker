# Скопируйте в server/deploy.env (файл в .gitignore) для автоматического деплоя без ввода пароля.
# Пароль root SSH на VPS (не путать с логином приложения Panko и mobile-installer.env).
CATTLE_TRACKER_SSH_PASSWORD=
